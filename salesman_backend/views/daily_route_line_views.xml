<?xml version="1.0"?>
<odoo>
    <record id="view_daily_route_line_tree" model="ir.ui.view">
        <field name="name">daily.route.line.tree</field>
        <field name="model">salesman.daily.route.line</field>
        <field name="arch" type="xml">
            <tree>
                <field name="sequence" string="الترتيب"/>
                <field name="customer_id" string="العميل"/>
                <field name="visited" string="تمت الزيارة"/>
            </tree>
        </field>
    </record>
</odoo>
